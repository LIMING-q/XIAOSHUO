# Changelog — 墨泉 · AI 小说生成器

所有显著变更均记录于此。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [2.0.0] — 2026-08-28

完整重写，从「能用的小工具」升级为「全功能小说创作工作台」。

### 🏗️ 架构

- **双引擎**：本地智能引擎（种子随机，零依赖离线可用）+ AI 大模型引擎（OpenAI 兼容接口，流式输出）
- **模块化 IIFE**：7 个 JS 模块（utils / store / content / prose / engine / ai / app），各自独立、可替换
- **零依赖**：运行时无任何第三方库，纯原生 JS + CSS

### ✨ 新增 — 创作向导

- 11 大题材（玄幻 / 仙侠 / 都市 / 科幻 / 武侠 / 悬疑 / 言情 / 历史 / 奇幻 / 末世 / 无限流）
- 6 种内置文风（古风 / 热血 / 悬疑 / 文艺 / 诙谐 / 史诗）+ **自定义文风**（用户喂入特色词汇 / 句式 / 替换词对，可保存多套切换）
- **4 套叙事模板**：三幕结构（默认）/ 英雄之旅 / 七点式 / 救猫咪
- 章节数自由设定（5–9999），一键随机设定 + 实时预览

### ✨ 新增 — 大纲

- 三幕结构自动生成 9–26 章，含标题 / 摘要 / 地点 / 幕别
- **章节拖拽排序**：拖拽重排后自动更新编号，伏笔埋设 / 回收索引自动位移
- **🔮 伏笔追踪面板**：列出全部伏笔的埋设章 / 回收章 / 状态（已回收 / 待回收 / 悬空），可筛选、可手动埋设
- 每章可独立设定目标字数

### ✨ 新增 — 角色卡

- 主角 + 盟友 + 对手 + 引路人，每张卡含样貌、性格、背景、目标、成长弧光
- **🔗 角色关系图**：SVG 交互式可视化，可拖拽节点布局，连线显示关系类型（盟友 / 宿敌 / 师徒 / 同门 / 情侣 / 血亲），点连线或「＋ 添加关系」编辑，生成时自动补全默认关系边
- **性格化对话**：12 种性格台词风格，不同角色对话口吻自然区分

### ✨ 新增 — 地点百科

- 自定义地点池（名称 / 描述 / 出场章节 / 关联角色）
- 本地引擎生成正文与大纲时**优先从用户地点池中选取场景**
- **章节生成后自动追踪**：地点名匹配成功时自动写入出场章节和关联角色
- 按题材一键填入热门地点（含对应描述）

### ✨ 新增 — 写作台

- 纸质感阅读编辑器，**逐段打字机生成**正文
- **下方预览区**：生成 / 续写 / 重写过程全部在文章下方展开，确认满意后再并入正文
- **多版本生成**：一次生成 3 个整章候选，选一个采用
- **多版本续写**：一次生成 3 个不同走向的续写候选
- **续写取消后可选处理**：丢弃 / 并入正文 / 保留为草稿
- **全文对比**：生成完成时与当前正文双栏对照，差异高亮（LCS 按句对比）
- **重写本章**：一键换文风重写，先在下方预览，再进入原稿 / 重写版对比模式
- **生成历史**：每次生成自动存快照（保留最近 5 份），可对比并回滚
- **字数目标进度环**：每章可独立设定目标字数，实时完成度显示，达标脉冲动画
- **自动保存草稿**：每 5 秒持久化，刷新或误关页面后提示恢复
- **全文查找**：`Ctrl+Shift+F` 跨章节搜索，结果按章节分组，点击跳转高亮定位
- **全局替换**：一键替换全书所有人名，同步更新角色卡与大纲
- **🔍 本地错别字检查**：纯规则扫描「的地得」误用与相邻重复词（含合法叠词白名单），可定位高亮或一键替换

### ✨ 新增 — AI 能力

- **多服务商接入**：DeepSeek / 智谱 GLM / 通义千问 / OpenAI / Moonshot / 自定义（兼容 OpenAI 格式）
- **模型自动读取**：填好 Base URL + API Key 后一键拉取模型列表
- **流式输出**：正文 / 续写走 SSE 流式，实时显示生成过程
- **自动重试**：可配置不重试 / 1 次 / 2 次，流式生成中断时保留已生成部分从断点续写，含锚点回声去重
- **生成取消**：AbortController 一键中断，重试等待期也可取消
- **每个模块独立切换**「本地模板 / AI 深度」
- **🤖 AI 审稿助手**：四维报告——节奏分析、对话点评（含 OOC 检测）、伏笔状态、可删减段落
- **🧪 AI 设定一致性检查**：通读全书报告时间线 / 人物行为 / 设定冲突 / 情节遗漏四类矛盾
- **💡 上下文灵感卡片**：22 张卡片按当前章节 beat 智能推荐，点击卡片可直接驱动续写
- **本地 CORS 代理**：零依赖 `proxy.js`，解决不支持浏览器直连的服务商

### ✨ 新增 — 书架与导出

- 多部小说 localStorage 持久化，进度条显示
- **断点续读**：卡片显示上次读到的章节，点击直接从该章打开
- **三种排序**：按更新时间 / 总字数 / 完成度
- **全文查找**：跨章节搜索
- 导出格式：TXT / Markdown / **EPUB 电子书**（标准 3.0 + 自动 SVG 封面，可直接导入 Kindle / Apple Books）
- **整本书 JSON 备份**：一键导出全部章节 / 大纲 / 角色到文件，另一台设备可导入继续创作
- **📊 写作统计面板**：总字数 / 每日写作量柱状图 / 连续写作天数 streak / 每日字数目标 / 章节字数分布直方图 / 年度写作日历热力图
- **📤 导出统计报告**：自包含 HTML 文件，双击即可分享到社交媒体
- **发布平台预设**：每章可标注发布状态（起点 / 番茄 / 晋江 / 纵横）

### ✨ 新增 — 专注模式与纸张

- **专注模式**：一键隐藏侧栏 / 灵感面板 / 工具栏，只留纸张居中写作，`Esc` 退出
- **6 种纸张质感**：纯白 / 宣纸米白 / 暖黄 / 羊皮纸 / 墨绿 / 深色护眼，带微噪点纹理
- 偏好持久化

### ✨ 新增 — 基础设施

- **PWA**：Service Worker 网络优先 + 缓存回退，可安装到桌面离线使用
- **🌓 深色 / 浅色主题**：一键切换，偏好持久化
- **移动端适配**：≤860px / ≤500px 两段断点，侧栏变顶栏、卡片单列
- **键盘快捷键**：`Ctrl+Enter` 生成 · `Ctrl+Shift+Enter` 续写 · `Ctrl+S` 保存 · `←/→` 切章 · `Ctrl+Shift+F` 查找

### ✨ 新增 — 测试

- **单元回归 84 项**（`tests/run.js`）：覆盖所有模块的纯函数逻辑 + 边界场景
- **全流程冒烟 10 项**（`tests/smoke.js`）：端到端创作流程验证
- **Playwright 端到端 2 项**（`tests/e2e.spec.js`）：真实浏览器 UI 交互测试
- 一键脚本：`npm test` / `npm run check` / `npm run test:all`
- **测试覆盖率文档**（`TEST_COVERAGE.md`）

---

## [1.0.0] — 初始版本

- 基础小说生成器（纯前端，单文件 `index.html`）
- 本地模板引擎：题材 × 文风 × 篇幅
- 简单大纲与章节生成
- localStorage 持久化
- TXT 导出

---

_此文件由维护者手动维护。_
